rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /almox_dados/{document} {
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
